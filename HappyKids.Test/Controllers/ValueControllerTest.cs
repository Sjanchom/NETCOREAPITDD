using Xunit;

namespace HappyKids.Test.Controllers
{
     class ValueControllerTest
    {
        [Fact]
         void InitialTest()
        {
            //var controller = new ValuesController().GetType();
        
            //var methodInfo = controller.GetMethod("Get",null);
            //var attributes = methodInfo.GetCustomAttributes(typeof(AuthorizeAttribute), true);
            //Assert.True(attributes.Any(), "No AuthorizeAttribute found on ChangePassword(ChangePasswordModel model) method");
            ////Assert.Equal(2,result.Count());
        }
    }
}
